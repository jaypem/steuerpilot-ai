[project]
name = "steuerpilot-backend"
version = "0.1.0"
description = "RAG-basierter Steuerberater-Assistent — Backend"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.135.3",
    "pydantic-settings>=2.13.1",
    "python-dotenv>=1.2.2",
    "uvicorn[standard]>=0.44.0",
]
